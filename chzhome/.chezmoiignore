README.md
.oh-my-zsh/cache/**

# Ignore HyperTerminal config file on incorrect platforms
{{ if or (eq .chezmoi.os "windows") (.chezmoi.kernel.osrelease | lower | contains "microsoft") }}
.hyper.js
{{ end }}

# Ignore files on Windows that don't apply
{{ if eq .chezmoi.os "windows" }}
.zshrc
{{ end }}

#!/bin/sh

set -eu

{{ if and (not .nopackages) (get . "games") -}}

brew bundle --file=/dev/stdin <<EOF
{{ range (.packages.darwin.homebrew.games.brews | default (list) | sortAlpha | uniq) -}}
brew "{{ . }}"
{{ end -}}
{{ range (.packages.darwin.homebrew.games.casks | default (list) | sortAlpha | uniq) -}}
cask "{{ . }}"
{{ end -}}
EOF

{{ end -}}

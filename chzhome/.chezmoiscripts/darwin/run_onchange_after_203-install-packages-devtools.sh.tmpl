#!/bin/sh

set -eu

{{ if and (not .nopackages) (get . "devtools") -}}

brew bundle --file=/dev/stdin <<EOF
{{ range (.packages.darwin.homebrew.devtools.brews | default (list) | sortAlpha | uniq) -}}
brew "{{ . }}"
{{ end -}}
{{ range (.packages.darwin.homebrew.devtools.casks | default (list) | sortAlpha | uniq) -}}
cask "{{ . }}"
{{ end -}}
EOF

{{ end -}}
